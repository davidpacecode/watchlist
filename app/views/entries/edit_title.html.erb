<%= turbo_frame_tag dom_id(@entry, :title) do %>
  <%= form_with(model: @entry, url: update_title_entry_path(@entry)) do |form| %>
    <% if @entry.errors.any? %>
      <div style="color: red">
        <h2><%= pluralize(entry.errors.count, "error") %> prohibited this entry from being saved:</h2>

        <ul>
          <% @entry.errors.each do |error| %>
            <li><%= error.full_message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

    <div>
      <wa-input name="entry[title]" value="<%= @entry.title %>" size="s"></wa-input>
    </div>

    <div>
      <div class="wa-cluster wa-gap-3xs">
        <wa-button appearance="outlined" type="submit" variant="neutral" size="xs">Save</wa-button>
        <wa-button appearance="outlined" href="<%= entries_path %>" variant="neutral" size="xs">Nevermind</wa-button>
      </div>
      <%# form.submit %>
    </div>
  <% end %>
<% end %>
